# 📖 คู่มือการใช้งาน Revit Elements Exporter

<div align="center">

![Version](https://img.shields.io/badge/Version-1.0.0-blue?style=flat-square)
![Revit](https://img.shields.io/badge/Revit-2026-orange?style=flat-square)

**ส่งออกข้อมูล Revit Elements พร้อมพิกัดและ Metadata**

</div>

---

## 📋 สารบัญ

1. [การติดตั้ง](#-การติดตั้ง)
2. [การเปิดใช้งาน](#-การเปิดใช้งาน)
3. [หน้าจอหลัก](#-หน้าจอหลัก)
4. [การตั้งค่า Export](#-การตั้งค่า-export)
5. [การกรอง Categories](#-การกรอง-categories)
6. [รูปแบบไฟล์](#-รูปแบบไฟล์)
7. [ข้อมูลที่ส่งออก](#-ข้อมูลที่ส่งออก)
8. [Dark Mode](#-dark-mode)
9. [แก้ไขปัญหา](#-แก้ไขปัญหา)

---

## 🔧 การติดตั้ง

### วิธีที่ 1: ใช้ Script อัตโนมัติ (แนะนำ)

```powershell
# เปิด PowerShell แล้วรันคำสั่ง
.\Deploy.ps1 -Configuration Release
```

### วิธีที่ 2: ติดตั้งด้วยตนเอง

1. คัดลอกไฟล์ต่อไปนี้ไปยัง `%AppData%\Autodesk\Revit\Addins\2026\`:

   - `RevitElementsExporter.dll`
   - `DocumentFormat.OpenXml.dll`
   - `DocumentFormat.OpenXml.Framework.dll`
   - `RevitElementsExporter.addin`

2. รีสตาร์ท Revit

### ตรวจสอบการติดตั้ง

หลังจากติดตั้งและเปิด Revit จะเห็นเมนู:

- **Add-Ins** → **External Tools** → **Export Elements**

---

## 🚀 การเปิดใช้งาน

1. เปิดไฟล์ Revit project ที่ต้องการ export
2. ไปที่เมนู **Add-Ins**
3. คลิก **External Tools**
4. เลือก **Export Elements**

---

## 🖥 หน้าจอหลัก

### ส่วนประกอบของหน้าจอ

```
┌─────────────────────────────────────────────┐
│  🏠 Header - ชื่อโปรแกรมและปุ่ม Theme      │
├─────────────────────────────────────────────┤
│  📊 Stats Card - แสดงสถิติ elements        │
│  ┌─────┬─────┬─────┬─────┐                 │
│  │Total│Point│Curve│ Cat │                 │
│  └─────┴─────┴─────┴─────┘                 │
├─────────────────────────────────────────────┤
│  ┌───────────────────┬──────────────┐       │
│  │ Format & Path     │ Category     │       │
│  │ • รูปแบบไฟล์     │ Filter       │       │
│  │ • ตำแหน่งไฟล์    │ • Walls      │       │
│  │                   │ • Doors      │       │
│  │ Info Box          │ • Windows    │       │
│  └───────────────────┴──────────────┘       │
├─────────────────────────────────────────────┤
│  v1.1.0           [ยกเลิก] [✓ Export]      │
└─────────────────────────────────────────────┘
```

---

## ⚙ การตั้งค่า Export

### 1. เลือกรูปแบบไฟล์

คลิกที่ dropdown "รูปแบบไฟล์" แล้วเลือก:

| รูปแบบ   | คำอธิบาย                   | ใช้สำหรับ            |
| -------- | -------------------------- | -------------------- |
| 📄 CSV   | Comma-Separated Values     | Excel, ฐานข้อมูล     |
| 📋 JSON  | JavaScript Object Notation | เว็บแอพ, API         |
| 📊 Excel | ไฟล์ .xlsx                 | รายงาน, การวิเคราะห์ |

### 2. ตั้งค่าตำแหน่งไฟล์

- ค่าเริ่มต้น: `Desktop\RevitAllElements.csv`
- คลิกปุ่ม 📁 เพื่อเลือกตำแหน่งใหม่
- นามสกุลไฟล์จะเปลี่ยนตามรูปแบบที่เลือกอัตโนมัติ

---

## 🔍 การกรอง Categories

### ใช้งาน Category Filter

ด้านขวาของหน้าจอจะแสดงรายการ Categories พร้อมจำนวน elements:

```
☑ Walls (1,234)
☑ Doors (567)
☑ Windows (890)
☐ Levels (10)
☐ Views (45)
```

### ปุ่มลัด

| ปุ่ม         | การทำงาน              |
| ------------ | --------------------- |
| **ทั้งหมด**  | เลือกทุก categories   |
| **ไม่เลือก** | ยกเลิกการเลือกทั้งหมด |

### Stats อัพเดทแบบ Real-time

เมื่อเลือก/ยกเลิก categories ตัวเลขด้านบนจะอัพเดททันที:

- **Total**: จำนวน elements ที่จะ export
- **Point**: elements ที่มี point location
- **Curve**: elements ที่มี curve location
- **Categories**: จำนวน categories ที่เลือก

---

## 📁 รูปแบบไฟล์

### CSV Format

```csv
Id,Category,Family,Type,Level,LocationType,X,Y,Z,StartX,StartY,StartZ,EndX,EndY,EndZ
123456,Walls,Basic Wall,Generic 200mm,Level 1,Curve,,,,-5.2345,10.1234,0.0000,8.7654,10.1234,0.0000
```

### JSON Format

```json
[
  {
    "Id": "123456",
    "Category": "Walls",
    "Family": "Basic Wall",
    "Type": "Generic 200mm",
    "Level": "Level 1",
    "LocationType": "Curve",
    "StartX": -5.2345,
    "StartY": 10.1234,
    ...
  }
]
```

### Excel Format

ไฟล์ .xlsx พร้อมใช้งานทันที มี headers ในแถวแรก

---

## 📊 ข้อมูลที่ส่งออก

### คอลัมน์ทั้งหมด

| คอลัมน์        | ประเภท | คำอธิบาย                             |
| -------------- | ------ | ------------------------------------ |
| `Id`           | Text   | Element ID                           |
| `Category`     | Text   | ชื่อ Category เช่น Walls, Doors      |
| `Family`       | Text   | ชื่อ Family (สำหรับ FamilyInstance)  |
| `Type`         | Text   | ชื่อ Type                            |
| `Level`        | Text   | Level ที่เชื่อมโยง                   |
| `LocationType` | Text   | Point / Curve / Curve-Unbound / None |
| `X, Y, Z`      | Number | พิกัดจุด (เมตร) - สำหรับ Point       |
| `StartX/Y/Z`   | Number | จุดเริ่มต้น (เมตร) - สำหรับ Curve    |
| `EndX/Y/Z`     | Number | จุดสิ้นสุด (เมตร) - สำหรับ Curve     |

### หน่วยพิกัด

> ⚠️ **สำคัญ**: พิกัดทั้งหมดแปลงเป็น **เมตร** อัตโนมัติ (จากฟุตใน Revit)

### ประเภท Location

| LocationType    | คำอธิบาย                | ตัวอย่าง Elements         |
| --------------- | ----------------------- | ------------------------- |
| `Point`         | มีพิกัดจุดเดียว         | Doors, Windows, Furniture |
| `Curve`         | มีจุดเริ่มต้นและสิ้นสุด | Walls, Lines, Beams       |
| `Curve-Unbound` | เส้นไม่มีขอบเขต         | Reference lines           |
| `None`          | ไม่มี location          | Views, Levels, Types      |

---

## 🌙 Dark Mode

### วิธีเปิดใช้งาน

คลิกปุ่ม 🌙 ที่มุมขวาบนของหน้าจอ

| โหมด  | ไอคอน | เหมาะสำหรับ            |
| ----- | ----- | ---------------------- |
| Light | 🌙    | การใช้งานทั่วไป        |
| Dark  | ☀️    | ลดแสงเมื่อทำงานกลางคืน |

---

## ❓ แก้ไขปัญหา

### ไม่เห็นเมนู Export Elements

1. ตรวจสอบว่าไฟล์ `.addin` อยู่ใน folder ที่ถูกต้อง
2. ตรวจสอบ path ใน `.addin` ว่าตรงกับที่ตั้ง DLL
3. รีสตาร์ท Revit

### Export ไม่ได้ / Error

1. ตรวจสอบว่ามี project เปิดอยู่
2. ตรวจสอบว่า path ที่เลือกมีสิทธิ์เขียนได้
3. ลองเปลี่ยนตำแหน่งไฟล์ไปยัง Desktop

### ไฟล์ไม่มีข้อมูล

- ตรวจสอบว่ามี categories ถูกเลือกอยู่
- ดูตัวเลข Total ด้านบน ถ้าเป็น 0 แสดงว่าไม่มี elements ที่ตรงเงื่อนไข

### พิกัดไม่ถูกต้อง

- พิกัดเป็นหน่วย **เมตร** (ไม่ใช่ฟุต)
- ตรวจสอบระบบพิกัดใน Revit (Project Base Point)

---

## 📞 ติดต่อ

หากพบปัญหาหรือต้องการ features เพิ่มเติม:

- เปิด Issue ใน GitHub repository
- หรือติดต่อผู้พัฒนาโดยตรง

---

<div align="center">

**Made with ❤️ for Revit Users**

</div>
